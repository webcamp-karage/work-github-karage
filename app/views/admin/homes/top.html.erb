<div class="container">
    <div class="row">
      <h2>注文履歴一覧</h2>
      <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th>購入日時</th>
            <th>購入者</th>
            <th>注文個数</th>
            <th>注文ステータス</th>
            <!--<th colspan="3"></th>-->
          </tr>
        </thead>
          <tbody>
          <% @orders.each do |order| %>
        <tr>
         <!--ここから下にそれぞれの購入日時→注文履歴詳細に飛ぶ、-->
         <!--購入者、購入個数、購入ステータスが表示させるコードを記述。-->
           <td>
            <%= link_to admin_order_path(order.id) do %>
             <%= order.created_at.strftime("%Y/%m/%d %H:%M:%S") %>
            <% end %>
           </td>
    
           <td>
           <%= order.customer.first_name %> <%= order.customer.last_name %>
           </td>
    
           <td>
         　<%= @total = order.order_details.inject(0) { |sum, order_detail| sum + order_detail.amount } %>
         　<!--inject→テーブルの中のカラムの合計を計算するメソッド-->
           </td>
    
           <td>
        　 <%= order.status_i18n %>
           </td>
        </tr>
          <% end %>
        </tbody>
      </table>
    </div>
</div>
